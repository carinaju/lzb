# ── Google Ads Sheet (Supermetrics export — must be publicly readable) ──
VITE_ADS_SHEET_ID=your_google_ads_sheet_id_here

# ── Wix Monthly Revenue Sheet ───────────────────────────────────────────
# Create a Google Sheet with columns: Month (YYYY-MM) | Revenue | Orders | AOV
# Export your Wix monthly revenue data there and share it publicly (Viewer).
# See setup instructions in the README.
VITE_WIX_SHEET_ID=your_wix_revenue_sheet_id_here
